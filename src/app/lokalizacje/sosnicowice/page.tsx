import {
  makeLocationMetadata,
  makeLocationPage,
} from "@/content/locations-shared";

export const metadata = makeLocationMetadata("sosnicowice");
export default makeLocationPage("sosnicowice");
